# 参与贡献

欢迎修复问题、补充文档、改进模型适配与平台兼容性。较大改动建议先发 Issue 说明使用场景和拟实现行为。

1. Fork 仓库，创建一个描述改动的分支。
2. 使用 Node.js 22+，运行 `npm ci`。桌面分析需要另外安装 FFmpeg。
3. 修改代码；凭据保存在被忽略的 `.env` 或桌面设置中。测试使用合成内容和假密钥，不依赖私人云服务或真实视频。
4. 执行 `npm test` 和 `npm run check:public`。涉及界面时在 Windows 执行 `npm run test:desktop`；涉及构建时执行 `npm run build:dir`。
5. 提交 PR，说明问题、修改后的行为和验证结果。截图请遮去个人账号、路径、报告和密钥。

配置新增后需要同步设置清单、`.env.example` 和说明文档。默认启动必须保持本地模式，不依赖开发者机器的绝对路径。不要提交编译后的运行时、下载的视频、截图中的用户数据或云部署凭据。

提交贡献表示你有权以本项目 MIT 许可证提供相应代码。引用第三方实现时保留来源和适用许可证。交流请围绕技术与使用场景，避免人身攻击和泄露他人信息。
