# 安全政策

当前维护 1.7.x 开源版本。请优先升级到该系列最新发布版本。

发现密钥泄露、权限绕过、任意代码执行或用户资料暴露时，请通过仓库 Security 页的 **Report a vulnerability** 私密报告。若该入口不可用，可先提交不含漏洞细节的 Issue 请求私密联系方式，不要在公开 Issue/PR 中贴出利用代码、实际密钥、Cookie、账号数据或用户视频。

请提供版本、系统、最小复现条件和影响范围，使用合成数据。维护者会在有修复或需要补充信息时跟进；本项目不承诺固定响应时限。

本地 API 仅用于桌面程序，保持回环监听与随机访问令牌；不要将其直接暴露到公网。模型输出、远程页面和导入内容均应视作不可信输入。设置页默认不返回密钥明文，单项查看受主窗口来源限制。

如果你误提交了凭据，先在供应商处撤销或轮换，再处理 Git 历史。删除文件或提交一个遮密版本不能撤销已经公开的密钥。

